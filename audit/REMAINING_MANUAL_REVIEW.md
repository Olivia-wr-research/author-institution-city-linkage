# Remaining Administrative Steps

No unresolved code-ownership or license blocker remains for the current repository contents. Before or immediately after creating the GitHub repository:

- Add the final `repository-code` URL to `CITATION.cff`.
- Add the actual first-release date to `CITATION.cff`.
- Confirm that the uploaded repository exactly matches this reviewed folder.
- Do not add restricted data, real credentials, or unreviewed third-party code in the initial commit.
