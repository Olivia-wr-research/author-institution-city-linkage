# Configuration

The public demonstration uses `config/config.example.json`, read by `scripts/run_example_pipeline.py`.

No API credentials are required or used. Real Scopus/OpenAlex/ROR adapters are outside this public demonstration and should remain private or be added later only after license and credential review.
