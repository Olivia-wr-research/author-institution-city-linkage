{
  "description": "Hard linkage failures and configured duplicate exclusions.",
  "required_fields": [
    "record_id",
    "author_id",
    "work_id",
    "institution_id",
    "exclusion_reason",
    "qa_notes"
  ]
}
